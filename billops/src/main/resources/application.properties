# Set the server port for the BillOps microservice
server.port=8082

spring.application.name=billops
spring.datasource.url=jdbc:postgresql://3.110.102.128:5432/production_db
spring.datasource.username=app_user
spring.datasource.password=AppPassword123
spring.datasource.driver-class-name=org.postgresql.Driver

# ==============================================================
# JPA / HIBERNATE
# ==============================================================
# Ensure Hibernate does not try to alter the schemas automatically 
# as it switches contexts. It can be dangerous in multi-tenant.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate Settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.springframework=INFO
logging.level.com.xammer.billops=DEBUG

# Log output to file and set log patterns for detail
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# AWS Configuration
# --- RENAMED PROPERTY BELOW TO MATCH CODE ---
cloudformation.template.s3.url=https://xamopscftemplate.s3.ap-south-1.amazonaws.com/billops-readonly-role.yml
# --- ENSURE THIS IS STILL HERE FROM PREVIOUS FIX ---
aws.region=ap-south-1
# ------------------------------------

# Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Hikari Connection Pool Settings for stability
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=200000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-test-query=SELECT 1

server.servlet.session.timeout=1d

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=notifications@xamops.com
spring.mail.password=biph daxm lnjt imer
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.profiles.active=local
server.servlet.session.timeout=1800s
app.login-url=https://billops.xamops.com/login 
spring.redis.port=6379
server.servlet.session.cookie.path=/

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.forward-headers-strategy=native
logging.level.org.springframework=INFO
logging.level.com.xammer.cloud=INFO
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web.cors=DEBUG
logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG

app.s3.bucket-name=xamops-document-bucket
app.s3.region=ap-south-1
# ==============================================================
# DEBUG LOGGING CONFIGURATION
# ==============================================================
# See the Security Context being loaded from the session
logging.level.org.springframework.security.web.context=DEBUG
logging.level.org.springframework.security.core.context=DEBUG

# See Session Repository details (Redis/JDBC)
logging.level.org.springframework.session=DEBUG

# See general web request handling
logging.level.org.springframework.web=DEBUG

# Keep your app logs at DEBUG
logging.level.com.xammer.billops=DEBUG