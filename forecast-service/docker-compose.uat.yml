version: '3.8'

services:
  spring-app:
    image: xamops-spring:uat
    container_name: xamops-spring-uat
    ports:
      - "8080:8080"
    environment:
      - FORECAST_SERVICE_URL=http://forecast-service:5002
      - SPRING_PROFILES_ACTIVE=uat
    depends_on:
      forecast-service:
        condition: service_healthy
    networks:
      - xamops-network

  forecast-service:
    build:
      context: ./forecast-service
      dockerfile: Dockerfile
    container_name: xamops-forecast-uat
    ports:
      - "5002:5002"
    environment:
      - FLASK_ENV=uat
      - LOG_LEVEL=INFO
      - PORT=5002
      - WORKERS=2
      - THREADS=4
      - TIMEOUT=180
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - xamops-network
    restart: unless-stopped

networks:
  xamops-network:
    driver: bridge
