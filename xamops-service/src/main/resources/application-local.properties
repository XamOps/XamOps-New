# ===================================================================
# LOCAL ENVIRONMENT CONFIGURATION
# ===================================================================

# --- Local PostgreSQL Database ---
# This line has the fix. It tells the application to use the UTC timezone.
# src/main/resources/application-local.properties

# --- Local PostgreSQL Database on EC2 ---
spring.datasource.url=jdbc:postgresql://${DB_HOST:13.204.166.9}:${DB_PORT:5432}/${DB_NAME:xamops_local_db}
spring.datasource.username=${DB_USER:xamops_local_user}
spring.datasource.password=${DB_PASS:XamOps@123}

# --- Other local configurations can remain the same ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
logging.level.org.springframework=INFO
logging.level.com.xammer.cloud=DEBUG
cors.allowed-origins=http://localhost:8080,http://127.0.0.1:5500,http://localhost:5173
app.frontend.url=http://localhost:5173


# --- Local API Keys ---
gemini.api.key=${GEMINI_API_KEY:AIzaSyDUHXg5yf3PrWdtvrHOhHQ9t5QKihoeCVA}
cloudformation.template.s3.url=https://xamopscftemplate.s3.ap-south-1.amazonaws.com/xamops-readonly-role.local.yml

# Point Thymeleaf to your frontend-app directory
spring.thymeleaf.prefix=file:frontend-app/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false